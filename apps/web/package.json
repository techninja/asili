{
  "name": "asili-webapp",
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp -r node_modules/@duckdb dist/ && cp *.html components dist/",
    "deploy": "aws s3 sync dist/ s3://asili-cdn",
    "test": "node tests/run-tests.js",
    "test:clear": "node tests/clear-storage.js"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.33.1-dev5.0"
  },
  "devDependencies": {
    "puppeteer": "^24.34.0"
  }
}
