// Pipeline DB not yet migrated to this repo — skip until asili-lab merge.
import { describe } from 'node:test';

describe('trait-db + pgs-db (pending pipeline migration)', { skip: true }, () => {});
